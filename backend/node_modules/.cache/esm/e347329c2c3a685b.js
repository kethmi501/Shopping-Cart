let express,cors,readdirSync,mongoose;_2c3‍.w("express",[["default",["express"],function(v){express=v}]]);_2c3‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_2c3‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_2c3‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);




require("dotenv").config();


// create express app
const app = express();

// db
mongoose
    .connect(process.env.DATABASE, {
        useNewUrlParser: true, useUnifiedTopology: true,
    })
    .then(() => console.log("DB CONNECTED"))
    .catch((err) => _2c3‍.g.console.log("DB CONNECTION ERR => ", err));


const corsOptions = {
    origin: '*',
    optionsSuccessStatus: 200,
    methods: ['GET', 'PUT', 'POST', 'DELETE', 'HEAD'],
    credentials: true,
};

app.use(cors(corsOptions));
app.use(express.json({limit: "5mb"}));

// route
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));


// port
const port = process.env.PORT || 8000;

app.listen(port, () => console.log(`Server is running on port ${port}`));
